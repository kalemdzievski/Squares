using UnityEngine;
using System.Collections;

public class CopyrightText : MonoBehaviour {

	// Use this for initialization
	void Start () {
		this.guiText.fontSize = (int)Screen.width / 30;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
